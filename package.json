{
    "name": "paulista-pdv-root",
    "version": "1.0.0",
    "scripts": {
        "dev": "npm run dev --prefix web_version",
        "build": "npm run build --prefix web_version",
        "start": "npm run start --prefix web_version",
        "install-all": "npm install --prefix web_version"
    }
}